const { test, expect } = require('@playwright/test');
const pages=['/','/team/','/start/','/gear/','/events/','/start/application.html'];
for(const url of pages){test(`${url} loads cleanly`,async({page})=>{const errors=[];page.on('console',m=>{if(m.type()==='error')errors.push(m.text())});page.on('pageerror',e=>errors.push(e.message));const r=await page.goto(url);expect(r.ok()).toBeTruthy();await page.waitForLoadState('networkidle');const overflow=await page.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth);expect(overflow).toBeLessThanOrEqual(1);expect(errors,errors.join('\n')).toEqual([]);expect(await page.locator('h1').count()).toBeGreaterThan(0)});}
test('mobile navigation works',async({page},testInfo)=>{test.skip(testInfo.project.name!=='mobile');await page.goto('/');const btn=page.locator('.menu-btn');await btn.click();await expect(btn).toHaveAttribute('aria-expanded','true');await expect(page.locator('.mobile-nav')).toHaveClass(/is-open/);await page.locator('.mobile-nav a').first().click();});
test('application produces prepared message without fake send',async({page})=>{await page.goto('/start/application.html');await page.fill('#name','Тест');await page.fill('#age','25');await page.selectOption('#experience',{label:'Опыта нет'});await page.selectOption('#gear',{label:'Пока ничего нет'});await page.fill('#contact','vk.com/test');await page.click('button[type=submit]');await expect(page.locator('[data-form-status]')).toHaveClass(/is-visible/);await expect(page.locator('[data-form-text]')).toContainText('Тест');});
test('primary internal links resolve',async({page,request})=>{await page.goto('/');const hrefs=await page.locator('a[href]').evaluateAll(as=>[...new Set(as.map(a=>a.getAttribute('href')).filter(h=>h&&!h.startsWith('http')&&!h.startsWith('#')&&!h.startsWith('mailto:')))]);for(const h of hrefs){const u=new URL(h,page.url());const r=await request.get(u.pathname);expect(r.status(),`${h} -> ${r.status()}`).toBeLessThan(400)}});